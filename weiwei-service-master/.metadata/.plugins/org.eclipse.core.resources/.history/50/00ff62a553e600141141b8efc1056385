package com.weiwei.service.processors.base;

import java.util.Map;

public abstract class BaseProcessor {
	public String process(Map scopes) {
		preProcess(scopes);
		executeProcess(scopes);
		postProcess(scopes);
	}
	
	protected void preProcess(Map scopes){
		
	}
}
